<%= flash[:notice] %>

<div class="show-contents>
   <p class="show-title">Title: <%= @book.title %></p>
   
   <p class="show-title">Body: <%= @book.body %></p>
   
   <p>
   <%= link_to "Edit",edit_book_path(@book),class: "edit_#{@book.id}"%>

   <%= link_to "Back", books_path, class: "back" %>
   </p>
</div>